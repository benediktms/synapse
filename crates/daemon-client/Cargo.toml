[package]
name = "daemon-client"
version.workspace = true
edition.workspace = true

[dependencies]
api = { path = "../api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9"

[dev-dependencies]
tempfile = "3"
