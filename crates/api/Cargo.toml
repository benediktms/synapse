[package]
name = "api"
version.workspace = true
edition.workspace = true

[dependencies]
domain = { path = "../domain" }
humantime = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
